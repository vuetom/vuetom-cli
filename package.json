{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "pnpm run -C docs dev",
    "build": "pnpm run -C docs build",
    "serve": "pnpm run -C docs serve"
  },
  "devDependencies": {
  },
  "dependencies": {
  }
}
